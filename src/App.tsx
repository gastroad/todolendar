import { FC } from "react"

import Logo from "@assets/images/logo.png"

const App: FC = () => {
    return (
        <>
            <img src={Logo} style={{ backgroundColor: "red" }} />
            asd
        </>
    );
};

export default App;
